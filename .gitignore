# Build output
/gno_obj/
*.sym
*.root
*.ROOT

# macOS
.DS_Store
*.swp
*~

# ORCA/M assembly intermediates (uppercase .A produced by assembler)
# Note: lowercase .a files are ORCA object modules and SHOULD be tracked
# when they are part of the source tree (e.g., lib/ORCALib/*.A)

# Probe files
/probe.*
